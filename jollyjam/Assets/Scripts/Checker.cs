using UnityEngine;

public class Checker : MonoBehaviour
{
    public bool isChecked;

    /*public void UpdateTile()
    {
        Vector3Int tileCoord = new Vector3Int((int) transform.position.x, (int) transform.position.y, 0);
        TileMapSpriteUpdater.Instance.UpdateTile(tileCoord);
        Debug.Log("Update tile");
    }*/
    
}
